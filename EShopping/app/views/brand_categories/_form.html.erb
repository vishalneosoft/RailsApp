<%= form_for(@brand_category) do |f| %>
  <% if @brand_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_category.errors.count, "error") %> prohibited this brand_category from being saved:</h2>

      <ul>
      <% @brand_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brand_id %><br>
    <%= f.text_field :brand_id %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
